---
label: gallery
hide_body: true
fields:
- type: text
  name: template
  label: template
  default: gallery
  hidden: true
- type: text
  name: heading
  label: heading
- type: text
  name: subheading
  label: subheading
- name: images
  type: field_group_list
  fields:
  - name: img
    type: text
    label: img
    description: James will need to upload these (they are stored on an image server to keep filesizes small for thumbails)
  config:
    min: 
    max: 
    labelField: 
  label: images
