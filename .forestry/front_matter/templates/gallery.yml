---
label: gallery
hide_body: true
fields:
- name: images
  type: field_group_list
  fields:
  - name: alt_text
    type: text
    config:
      required: true
    label: alt text
    default: art therapy pei
    description: "(This is used for SEO) - use it to create tags that might show up
      in google"
  - name: img
    type: file
    config:
      maxSize: '5'
    label: img
  config:
    min: 
    max: 
    labelField: 
  label: images
